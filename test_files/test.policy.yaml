# We support simpler policies with apiVersion and kind optional
metadata:
  name: file-open-home
spec:
  scope:
    - global
  rules:
    - event: security_file_open
      filters:
        - args.pathname=/home/*
